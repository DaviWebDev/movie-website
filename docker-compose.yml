services:
  movie-website:
    build:
      context: .
      target: dev

    env_file:
      - .env

    ports:
      - "4321:4321"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - CHOKIDAR_USEPOLLING=true
